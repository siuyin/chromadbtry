[project]
name = "chromadbtry"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chromadb>=1.1.1",
    "ollama>=0.6.0",
]

[dependency-groups]
dev = [
    "mypy>=1.18.2",
]
